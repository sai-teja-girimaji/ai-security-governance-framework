# Prompt Injection Defense Standards

> Technical controls for defending AI systems in SOC environments against prompt injection attacks

**Scope:** AI investigation agents, LLM-powered triage systems, and RAG-based knowledge retrieval deployed within SOC environments

---

## Overview

Prompt injection is the most operationally critical LLM security risk in a SOC context. Unlike most injection attacks, prompt injection does not require network access or code execution. The attack surface is the data the AI system is designed to process: log entries, alert payloads, email content, file metadata, and threat intelligence feeds.

An AI investigation agent that processes attacker-controlled data is processing a potential attack vector. This document defines the defensive controls required to harden AI systems against prompt injection in SOC environments.

---

## Attack Taxonomy

### Direct Prompt Injection

The attacker has direct input access to the AI system and submits crafted instructions in their query.

**SOC Relevance:** Lower risk in typical SOC deployments since direct input comes from authenticated analysts. Relevant for AI tools accessible through ticketing systems or collaboration platforms where input sources are broader.

### Indirect Prompt Injection

The attacker embeds instructions in content the AI system processes as data: log entries, file names, email subjects, document contents, or web pages fetched during investigation.

**SOC Relevance:** High. This is the primary attack vector. A threat actor who knows the organization uses AI-assisted investigation can embed instructions in:
- Log messages generated by compromised systems
- Email subjects and bodies in phishing investigations
- File names and metadata on analyzed malware samples
- DNS query strings from infected hosts
- HTTP headers in web traffic investigations

### RAG Poisoning

The attacker injects malicious content into the knowledge base used by a Retrieval-Augmented Generation system. When the system retrieves this content to answer a query, the injected instructions execute in the LLM's context.

**SOC Relevance:** High for organizations using RAG-based threat intelligence or playbook retrieval. An attacker who can write to a knowledge source (shared documents, wiki, ticketing system) can poison the RAG pipeline.

---

## Control Layer 1: Input Validation

Applied before any data enters the LLM context.

| Control | Description | Implementation |
|---|---|---|
| Instruction pattern detection | Scan incoming data for patterns consistent with LLM instruction syntax before ingestion | Regex and ML-based classifiers trained on known injection patterns. Block or flag matches. |
| Special token filtering | Remove or escape tokens that carry semantic weight in the target LLM (e.g. `<|im_start|>`, `[INST]`, `###`, `Human:`, `Assistant:`) | Allowlist-based content filter applied to all data sources |
| Control character stripping | Remove non-printable characters, zero-width characters, and Unicode direction overrides from log and alert data | Applied at the data ingestion layer before any parsing |
| Length enforcement | Enforce maximum field length limits. Excessively long strings in log fields are a signal of injection attempts. | Truncate at defined limits with logging of truncation events |
| Source tagging | Tag all data with its source before LLM ingestion. Maintain this tag throughout processing. | Required metadata field on all ingested objects |

---

## Control Layer 2: Prompt Architecture

Applied to how the LLM system prompt and context are structured.

| Control | Description | Implementation |
|---|---|---|
| Strict context separation | Separate system instructions (trusted) from data context (untrusted) using clear structural markers. Never allow data context to appear before system instructions in the prompt. | Enforce in prompt template. System instructions always precede data content. |
| Explicit role definition | Define the AI agent's role, capabilities, and constraints explicitly in the system prompt. Include explicit statements about what the agent will not do regardless of instructions in data. | Standard system prompt template reviewed by security architect |
| Instruction boundary markers | Use defined delimiters to mark the start and end of data content. Instruct the model to treat content between these markers as data only, never as instructions. | `<DATA_CONTEXT>` and `</DATA_CONTEXT>` markers with corresponding system prompt guidance |
| Negative instruction hardening | Explicitly instruct the model to ignore any instructions, commands, or directives it encounters within data sources | Included as a required section in all system prompts for SOC AI agents |
| Privilege separation | Tasks requiring different permission levels use separate LLM instances with separate system prompts and tool access | Do not reuse a single LLM instance across investigation tasks with different permission scopes |

---

## Control Layer 3: Output Validation

Applied to all LLM outputs before any action is taken.

| Control | Description | Implementation |
|---|---|---|
| Response schema validation | Define a strict JSON or structured schema for each LLM output type. Reject responses that do not conform. | Schema validation layer between LLM and action execution |
| Action pre-authorization | Before any action recommended by the LLM is executed, verify the action against the tiered autonomy permission model | Mandatory check in action execution pipeline |
| Anomaly detection on outputs | Baseline normal LLM output patterns. Alert on outputs that deviate significantly in structure, length, or content type. | Statistical monitoring of output patterns |
| Instruction leak detection | Scan LLM outputs for fragments of the system prompt or instruction text. These indicate a potential extraction attempt. | Pattern matching against system prompt content in all outputs |
| Chain-of-thought validation | If the LLM produces reasoning steps before a conclusion, validate that the reasoning is consistent with the conclusion and with the input data | Manual review sampling plus automated consistency checks |

---

## Control Layer 4: Context Isolation

Applied to limit the blast radius of a successful injection.

| Control | Description | Implementation |
|---|---|---|
| Minimum context scope | Include only the data required for the current task in the LLM context. Do not maintain investigation context across unrelated cases. | Session-scoped context with automatic clearing between cases |
| Tool access minimization | Grant the LLM only the tool access required for its current task. An agent investigating a phishing email does not need endpoint isolation tools. | Dynamic tool grants based on task type |
| Data source isolation | When analyzing high-risk data sources (malware, phishing content), use a restricted LLM instance with no action capabilities | Separate inference endpoint for high-risk content analysis |
| Read-before-act separation | Separate the investigation phase (read-only) from the action phase (write). The model that analyzes data does not directly execute actions. | Two-stage pipeline: analysis model passes recommendations to a separate action execution layer |

---

## Control Layer 5: Monitoring and Detection

Applied to detect injection attempts and measure control effectiveness.

| Signal | Detection Approach | Response |
|---|---|---|
| Instruction patterns in data sources | Real-time scan of all ingested data for known injection patterns | Log, alert, and flag the data source for investigation |
| AI output deviation from baseline | Statistical monitoring of output structure and content against established baseline | Alert and queue for human review if deviation exceeds threshold |
| Unexpected tool calls | Alert when the AI agent calls a tool that is not part of the expected workflow for the current alert type | Suspend agent action, escalate to human analyst |
| System prompt fragments in output | Pattern match against system prompt content in all LLM outputs | Immediate alert, treat as active extraction attempt |
| Cross-case data leakage | Monitor for content from previous investigations appearing in current session outputs | Alert on any cross-case data reference |
| Data source behavioral change | Alert when a previously clean log source begins producing content that triggers injection pattern detection | Investigate source compromise |

---

## Testing Requirements

### Pre-Deployment Testing

Before any AI SOC system is permitted to take autonomous action, the following injection tests must be completed and documented:

| Test | Description | Pass Criteria |
|---|---|---|
| Known payload library | Test against a library of known prompt injection payloads | System detects and blocks all known payloads without degrading legitimate processing |
| Log-embedded injection | Embed injection payloads in synthetic log entries matching the format of each production log source | Agent processes log data correctly without executing injected instructions |
| RAG poisoning simulation | If using RAG, inject instruction payloads into test knowledge base entries | System does not execute injected instructions from retrieved content |
| System prompt extraction | Attempt to extract system prompt content through crafted queries | System prompt content does not appear in outputs |
| Permission boundary bypass | Attempt to use injected instructions to execute Tier 1 restricted actions | Tier 1 protections cannot be bypassed through any injection vector |

### Ongoing Testing

| Activity | Frequency |
|---|---|
| Payload library update and retest | Quarterly |
| Red team injection exercise | Bi-annually |
| New data source injection assessment | Before each new log source is connected to the AI system |

---

## References

- OWASP Top 10 for LLM Applications: https://owasp.org/www-project-top-10-for-large-language-model-applications/
- NIST AI RMF 1.0: https://doi.org/10.6028/NIST.AI.100-1
